# Sandbox Content Contract

The lightweight browser sandbox must support:
- HTML, CSS, JavaScript (Vanilla only)
- Offline-capable execution
- Deterministic expectations
- No external CDNs unless explicitly taught
- No parent-window escapes or unrestricted networking
